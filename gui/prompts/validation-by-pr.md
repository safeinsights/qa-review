/qa-validate The browser is open (not yet logged in) against {{target}}. Validate PR {{pr}} of safeinsights/management-app: read the PR (gh) to learn what changed and identify the Jira ticket it implements (check the title, branch name, and body; ask me if you can't tell), read that ticket (jira-atlassian MCP) for its acceptance criteria, infer the role and log in via `qar session-login --role <role>` (ask me if unclear), then drive the browser via the chrome-devtools MCP to verify the acceptance criteria. Give a clear PASS/FAIL verdict.
